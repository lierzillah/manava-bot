const { Users, UsersEvents } = require('./users');
const {
  Broadcasts,
  BroadcastContents,
  BroadcastLogs,
} = require('./broadcasts');

module.exports = {
  Users,
  Auto,
  Booking,
  Repair,
  Routes,
  Tasks,
  RoutePoints,
  RouteStations,
};
