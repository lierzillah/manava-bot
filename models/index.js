const { Users, UsersEvents } = require('./users');
const {
  Broadcasts,
  BroadcastContents,
  BroadcastLogs,
} = require('./broadcasts');

const {
  Blocks,
  ContentTranslations,
  Buttons,
  ContentButtonTranslations,
} = require('./content');

module.exports = {
  Users,
  UsersEvents,
  Broadcasts,
  BroadcastContents,
  BroadcastLogs,
  Blocks,
  ContentTranslations,
  Buttons,
  ContentButtonTranslations,
};
